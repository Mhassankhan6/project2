@model IEnumerable<BMS.Models.AccountHolderType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.HolderTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.HolderTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.HolderTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.HolderTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.HolderTypeID })
        </td>
    </tr>
}

</table>
